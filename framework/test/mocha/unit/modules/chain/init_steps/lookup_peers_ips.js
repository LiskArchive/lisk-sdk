/* eslint-disable mocha/no-pending-tests */

describe('init_steps/lookup_peers_ips', () => {
	it('should return empty array if peers are not enabled');

	describe('for each peer', () => {
		it('should return peer if provided peer ip isIPv4');
		it('should look for peer IP');
		it('should assign IP to the peer');
		it('should log error if there is any');
		it('should return modified peer');
	});
});
