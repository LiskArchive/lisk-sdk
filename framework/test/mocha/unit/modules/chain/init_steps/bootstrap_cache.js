/* eslint-disable mocha/no-pending-tests */

describe('init_steps/bootstrap_cache', () => {
	it('should bootstrap the cache if it is enabled');
	it('should resolved without error if cache id disabled');
	it('should log message if cache is disabled');
});
