describe('#setupProcessHandlers', () => {
	// TODO: Figure out how to test this function in a future Issue
	// eslint-disable-next-line jest/no-disabled-tests
	it.skip('should register required events in process.once()', () => {});
});
