'use strict';

describe('lisk_error_handler', function () {

	it('should be a factory function that names 2 arguments');

	it('should create a middleware accepting 2 arguments');

	it('should accept handle500Errors config options');

	it('should skip < 400 errors');

	it('should handle 400 >= errors');

	it('should handle 500 errors and override response');
});
